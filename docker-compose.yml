version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      # Ensures Nitro listens on the expected port.
      PORT: 8080
      NITRO_PORT: 8080
